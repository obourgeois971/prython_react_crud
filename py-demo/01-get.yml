info:
  name: 01-get
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/products
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
